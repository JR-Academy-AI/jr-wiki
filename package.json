{
  "name": "jr-wiki",
  "type": "module",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "bun run build.ts",
    "build:data": "bun run build/pipelines/ai-daily.pipeline.ts && bun run build/pipelines/uni-news.pipeline.ts && bun run build/pipelines/uni-events.pipeline.ts",
    "build:ai-daily": "bun run build/pipelines/ai-daily.pipeline.ts",
    "build:uni-news": "bun run build/pipelines/uni-news.pipeline.ts",
    "build:uni-events": "bun run build/pipelines/uni-events.pipeline.ts",
    "render:posters": "bun run scripts/render-ai-news-posters.mjs",
    "dev": "bun run build.ts && bun run render:posters && bun run serve",
    "serve": "bunx serve dist -l 4321",
    "sync": "bun run build.ts && bun run sync-to-db.ts"
  },
  "devDependencies": {
    "puppeteer-core": "^23.0.0",
    "serve": "^14.2.0"
  }
}
