{
  "name": "jr-wiki",
  "type": "module",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "bun run build.ts",
    "dev": "bun run build.ts && bun run serve",
    "serve": "bunx serve dist -l 4321"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
